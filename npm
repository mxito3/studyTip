1.切换源
  sudo npm install -g nrm
  nrm是npm源的管理器
  用法：
    nrm ls   //列出源
    nrm use sourceName   //sourceName是ls中的某一个
2.权限问题
  修改权限不行的情况下
  npm install --unsafe-perm ...
3. nvm
wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.31.4/install.sh | bash
